{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["image", "policy", "rbac", "secretName"],
  "properties": {
    "image": {
      "type": "object",
      "required": ["repository"],
      "properties": {
        "repository": {"type": "string", "minLength": 1},
        "tag": {"type": "string"},
        "pullPolicy": {"enum": ["Always", "IfNotPresent", "Never"]}
      }
    },
    "rbac": {
      "type": "object",
      "required": ["targetNamespaces"],
      "properties": {
        "create": {"type": "boolean"},
        "targetNamespaces": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        },
        "clusterWide": {"type": "boolean"}
      }
    },
    "policy": {
      "type": "object",
      "required": ["users"],
      "properties": {
        "users": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": ["allowed_chats", "manifest"],
            "properties": {
              "allowed_chats": {
                "type": "array",
                "minItems": 1,
                "items": {"type": "integer"}
              },
              "manifest": {"type": "object"}
            }
          }
        }
      }
    },
    "secretName": {"type": "string", "minLength": 1}
  }
}
